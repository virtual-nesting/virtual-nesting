# Sliced Design 🌈

> Життя переможе смерть, а світ – темряву. 🇺🇦

**Sliced Design** — методология разработки файловой структуры проектов. Основана на опыте разработки приложений на React, но подходит для любых проектов на JavaScript.

> **Перевод на другие языки**
>
> Если у вас есть возможность, пожалуйста, внесите вклад и помогите перевести этот документ на другие языки!

## Содержание

1. [Проблема](#проблема)
1. [Решение](#решение)
1. [Пример структуры](#пример-структуры)
1. [Быстрый старт](#быстрый-старт)
1. [Концепция](#концепция)
   - [Супергруппа](#супергруппа)
   - [Группа](#группа)
   - [Юнит](#юнит)
   - [Дополнительно](#дополнительно)
1. [FAQ](#faq)
1. [Обратная связь](#обратная-связь)
1. [Контрибьюторы](#контрибьюторы)

## Проблема

Чаще всего в проектах используют **простую файловую структуру**, в которой файлы группируются по типу.

Для примера, файловая структура приложения на React может выглядеть так:

> ```
> src
> |-- api
> |-- assets
> |-- components
> |-- contexts
> |-- helpers
> |-- hooks
> |-- values
> ```

Файлы и директории с файлами, относящиеся к компонентам, помещаются в `components`, к хелперам — в `helpers`, к значениям — в `values` и т.д.

В **маленьких проектах** простая файловая структура обычно подходит и не вызывает проблем.

Но в **средних-больших**, с увеличением количества файлов и директорий, начинают возникать сложности:

- **Необходимо бороться с коллизией имён файлов и директорий**

  Поскольку в средних-больших проектах много файлов и директорий, рано или поздно возникает проблема коллизии имён. Обычно её решают двумя способами.

  Первый способ — держать файлы и директории на одном уровне вложенности, но усложнить их имена. Например, добавить префиксы.

  Недостаток этого подхода заключается в том, что со временем формируются длинные списки файлов и директорий. Их **долго листать**, в них **сложно найти** нужное. А сложные имена **менее удобно читать** и **обращаться к ним**.

  > ```
  > components
  > |-- (20 директорий, начинающихся с "editor")
  > |-- (10 директорий, начинающихся с "emoji")
  > |-- (50 директорий, начинающихся с "home")
  > |-- (30 директорий, начинающихся с "post")
  > ```

  Второй способ — использовать несколько уровней вложенности.

  Недостаток этого подхода заключается в том, что возникает неконтролируемая вложенность. Она **усложняет навигацию** по проекту и **оценку комплексности проекта и отдельных функций**.

  > ```
  > # с виду довольно просто, да?
  >
  > components
  > |-- editor
  > |-- emoji
  > |-- home
  > |-- post
  > ```

  > ```
  > # упс!
  >
  > components
  > |-- editor
  > |   |-- actions
  > |   |-- editor
  > |   |-- text-area
  > |-- emoji
  > |   |-- picker
  > |-- home
  > |   |-- layout
  > |   |-- posts
  > |       |-- create
  > |       |   |-- editor
  > |       |-- layout
  > |-- post
  >     |-- actions
  >     |-- content
  >     |   |-- content
  >     |   |-- image
  >     |   |   |-- layout
  >     |   |   |-- content-image
  >     |   |-- text
  >     |   |   |-- layout
  >     |   |   |-- content-text
  >     |   |-- video
  >     |       |-- layout
  >     |       |-- content-video
  >     |-- likes
  >     |-- profile-image
  >     |-- reply
  >         |-- editor
  >         |-- layout
  > ```

- **Сложно отследить связи между файлами**

  Если вы работаете над некоторой функцией в средних-крупных проектах, **понять, какие файлы относятся к этой функции, и где они располагаются — сложно**. Это происходит из-за слабой группировки файлов. Чаще всего, единственный вариант — изучить исходный код и составить представление в голове.

  Эта проблема в разной степени усугубляется, в зависимости от того, какой способ борьбы с коллизиями имён вы выбрали — сложные имена и длинные списки или вложенные директории.

## Решение

**Sliced Design** вводит несколько терминов (**юниты**, **группы**, **супергруппы**) и определяет некоторые правила, чтобы решить эту проблему.

Преимущества **Sliced Design**:

- подходит для любых проектов на JavaScript 🌠
- легко понять и использовать, простые термины и правила 🍬
- решает проблему коллизии имён файлов и директорий 🎯
- без сложных имён и длинных списков 🍝
- без неконтролируемой вложенности 🪆
- обеспечивает сильную группировку файлов 📎
- фиксированная физическая вложенность во всём проекте — максимум 3 уровня 📌
- возможность визуально изучить, какие файлы относятся к определённой функции приложения, без изучения исходного кода 🧩
- возможность визуально оценить комплексность проекта в целом и отдельных функций 🎇

## Пример файловой структуры

Файловая структура проекта на React, разработанная с применением **Sliced Design**, может выглядеть так:

> ```
> # в src содержатся супергруппы, они группируют юниты по функциям
>
> src
> |-- editor
> |-- emoji
> |-- home
> |-- home.posts
> |-- home.posts.create
> |-- home.shared
> |-- post
> |-- post.reply
> |-- post.shared
> |-- shared
> ```

> ```
> # в супергруппах содержатся группы, они группируют юниты по типу
>
> src
> |-- home
>     |-- components
>     |-- helpers
>     |-- index.js
> ```

> ```
> # юниты могут быть директориями с файлами
>
> src
> |-- home
>     |-- components
>         |-- layout
>             |-- component.jsx
>             |-- index.js
>             |-- styles.js
>
> # или просто файлами
>
> src
> |-- home
>     |-- helpers
>         |-- page.js
> ```

Посмотреть полный пример можно на [CodeSandbox](https://codesandbox.io/s/sliced-design-react-9qstoo).

## Быстрый старт

> Для тех, кто не любит скучную теорию. 🚀

### Свойства

<table>
  <tr>
    <th></th>
    <th>Супергруппа</th>
    <th>Группа</th>
    <th>Юнит</th>
  </tr>
  <tr>
    <td><b>Значение</b></td>
    <td>Директория, группирует юниты <b>по функции</b></td>
    <td>Директория, группирует юниты <b>по типу</b></td>
    <td>Директория с файлами или файл, содержит код и другие ресурсы</td>
  </tr>
  <tr>
    <td><b>Расположение</b></td>
    <td>В <code>src</code></td>
    <td>Внутри супергрупп</td>
    <td>Внутри групп</td>
  </tr>
  <tr>
    <td><b>Вложенность</b></td>
    <td colspan="3" align="center">Физически отсутствует, <b>выражается на уровне имён</b></td>
  </tr>
</table>

### Правила

<table>
  <tr>
    <th>К чему применяется</th>
    <th>Правило</th>
  </tr>
  <tr>
    <td>Супергруппы / юниты (кроме юнитов-файлов)</td>
    <td>Обязателен <b>централизованный экспорт</b> — структура должна содержать индексный модуль, любое обращение к её ресурсам должно осуществляться через него.</td>
  </tr>
  <tr>
    <td>Супергруппы</td>
    <td>В любом модуле супергруппы разрешён импорт ресурсов из другой супергруппы <b>1-го уровня вложенности</b>, с учётом централизованного экспорта.</td>
  </tr>
  <tr>
    <td>Группы</td>
    <td>В любом модуле группы разрешён импорт ресурсов из другой группы <b>1-го уровня вложенности</b>, в пределах той же супергруппы.</td>
  </tr>
  <tr>
    <td>Вложенные супергруппы</td>
    <td>В любом модуле супергруппы разрешён <b>восходящий импорт</b> — ресурсы из дочерней супергруппы можно импортировать в родительскую, с учётом централизованного экспорта.</td>
  </tr>
  <tr>
    <td>Вложенные группы</td>
    <td>В любом модуле группы разрешён <b>восходящий импорт</b> — ресурсы из дочерней группы можно импортировать в родительскую, в пределах той же супергруппы.</td>
  </tr>
  <tr>
    <td>Вложенные супергруппы / группы</td>
    <td>Если к ресурсам необходимо иметь доступ одновременно в родительской и дочерних структурах, их необходимо расположить в структуре <code>*.shared</code>.</td>
  </tr>
</table>

### Исключения

- **Супергруппы `*.shared`**

  Используются для хранения ресурсов, к которым необходимо иметь доступ одновременно в родительской и дочерних супергруппах.

  - ресурсы можно использовать в родительской супергруппе и всех вложенных в неё
  - не действует правило централизованного экспорта
  - не может иметь собственные вложенные супергруппы

- **Группы `*.shared`**

  Используются для хранения ресурсов, к которым необходимо иметь доступ одновременно в родительской и дочерних группах.

  - ресурсы можно использовать в родительской группе и всех вложенных в неё
  - не может иметь собственные вложенные группы

## Концепция

> Для тех, кому интересны детали и примеры. 🔎

### Супергруппа

Директория, группирует юниты **по функции**. Создаёт новое пространство имён.

> ```
> # пример супергруппы
>
> src
> |-- editor
>     |-- components
>     |-- values
>     |-- index.js
> ```

#### Расположение

Супергруппы располагаются в директории `src`.

#### Вложенность

Супергруппы могут быть вложенными. Вложенность выражается на уровне **имён директорий**, через перечисление цепочки родительских супергрупп через точку.

> ```
> src
> |-- home
> |-- home.posts
> |-- home.posts.create
> |-- post
> |-- post.reply
> ```

#### Правила

- Для супергрупп обязателен **централизованный экспорт**. Супергруппа должна содержать индексный модуль, любое обращение к её ресурсам должно осуществляться через него.

  > <code>src/<b>editor</b>/components/actions/component.jsx</code>:
  >
  > ```js
  > // ✅ так можно
  >
  > import { Picker } from "../../../emoji";
  >
  > // ❌ так нельзя, прямое обращение ресурсу супергруппы
  >
  > import Picker from "../../../emoji/components/picker";
  > ```

- В любом модуле супергруппы разрешён импорт ресурсов из другой супергруппы **1-го уровня вложенности**, с учётом централизованного экспорта.

  > <code>src/<b>home</b>/components/layout/component.jsx</code>:
  >
  > ```js
  > // ✅ так можно
  >
  > import Post from "../../../post";
  >
  > // ❌ так нельзя, обращение к супергруппе 2-го уровня вложенности
  >
  > import PostReply from "../../../post.reply";
  > ```

- В любом модуле супергруппы разрешён **восходящий импорт** — ресурсы из дочерней супергруппы можно импортировать в родительскую, с учётом централизованного экспорта.

  > <code>src/<b>home</b>/components/layout/component.jsx</code>:
  >
  > ```js
  > // ✅ так можно
  >
  > import Posts from "../../../home.posts";
  > ```

  > <code>src/<b>home.posts</b>/components/layout/component.jsx</code>:
  >
  > ```js
  > // ❌ так нельзя, импорт из родительской супергруппы в дочернюю
  >
  > import { HomeContext } from "../../../home";
  >
  > // ✅ но так можно
  >
  > import HomeContext from "../../../home.shared/contexts/home";
  > ```

- Если к ресурсам необходимо иметь доступ одновременно в родительской и дочерних супергруппах, их необходимо расположить в супергруппе `*.shared`.

#### Исключения

- **Супергруппы `*.shared`**

  Используются для хранения ресурсов, к которым необходимо иметь доступ одновременно в родительской и дочерних супергруппах.

  - ресурсы можно использовать в родительской супергруппе и всех вложенных в неё
  - не действует правило централизованного экспорта
  - не может иметь собственные вложенные супергруппы

  > <code>src/<b>home.posts</b>/components/layout/component.jsx</code>:
  >
  > ```js
  > // ✅ так можно
  >
  > import HomeContext from "../../../home.shared/contexts/home";
  >
  > // ❌ так нельзя, супергруппа `home.posts` не имеет доступ к ресурсам супергруппы `post.shared`
  >
  > import PostContext from "../../../post.shared/contexts/post";
  > ```

### Группа

Директория, группирует юниты **по типу**. Создаёт новое пространство имён.

> ```
> # пример группы
>
> src
> |-- editor
>     |-- components
>         |-- actions
>         |-- editor
>         |-- text-area
> ```

#### Расположение

Группы располагаются внутри супергрупп.

#### Вложенность

Группы могут быть вложенными. Вложенность выражается на уровне **имён директорий**, через перечисление цепочки родительских групп через точку.

> ```
> src
> |-- post
>     |-- components
>     |-- components.content.image
>     |-- components.content.text
>     |-- components.content.video
>     |-- helpers
>     |-- helpers.player
> ```

#### Правила

- В любом модуле группы разрешён импорт ресурсов из другой группы **1-го уровня вложенности**, в пределах той же супергруппы.

  > <code>src/post/<b>components.content.video</b>/content-video/component.jsx</code>:
  >
  > ```js
  > // ✅ так можно
  >
  > import player from "../../helpers/player";
  >
  > // ❌ так нельзя, обращение к группе 2-го уровня вложенности
  >
  > import player from "../../helpers.player/player";
  > ```

- В любом модуле группы разрешён **восходящий импорт** — ресурсы из дочерней группы можно импортировать в родительскую, в пределах той же супергруппы.

  > <code>src/post/<b>helpers</b>/player.js</code>:
  >
  > ```js
  > // ✅ так можно
  >
  > import player from "../helpers.player/player";
  > ```

  > <code>src/post/<b>helpers.player</b>/playback.jsx</code>:
  >
  > ```js
  > // ❌ так нельзя, импорт из родительской группы в дочернюю
  >
  > import math from "../helpers/math";
  >
  > // ✅ но так можно
  >
  > import math from "../helpers.shared/math";
  > ```

- Если к ресурсам необходимо иметь доступ одновременно в родительской и дочерних группах, их необходимо расположить в группе `*.shared`.

#### Исключения

- **Группы `shared`**

  Используются для хранения ресурсов, к которым необходимо иметь доступ одновременно в родительской и дочерних группах.

  - ресурсы можно использовать в родительской группе и всех вложенных в неё
  - не может иметь собственные вложенные группы

  > <code>src/post/<b>components</b>/content/component.jsx</code>:
  >
  > ```js
  > // ✅ так можно
  >
  > import Backdrop from "../../components.shared/backdrop";
  >
  > // ❌ так нельзя, группа `components` не имеет доступ к ресурсам группы `heleprs.shared`
  >
  > import math from "../../helpers.shared/math";
  > ```

### Юнит

Директория с файлами или файл, содержит код и другие ресурсы.

> ```
> # примеры юнитов
>
> # юнит-директория
>
> src
> |-- editor
>     |-- components
>         |-- text-area
>             |-- component.jsx
>             |-- index.js
>             |-- styles.js
>
> # юнит-файл
>
> src
> |-- editor
>     |-- values
>         |-- limits.js
> ```

#### Расположение

Юниты располагаются внутри групп.

#### Вложенность

Файлы внутри юнита могут быть вложенными. Вложенность выражается на уровне **имён файлов**, через перечисление цепочки родительских директорий через точку.

> ```
> src
> |-- editor
>     |-- components
>         |-- actions
>             |-- assets.emoji-icon.svg
>             |-- assets.gif-icon.svg
>             |-- assets.media-icon.svg
>             |-- component.jsx
>             |-- index.js
>             |-- styles.js
> ```

#### Правила

- Для юнитов обязателен **централизованный экспорт** (кроме юнитов-файлов). Юнит должен содержать индексный модуль, любое обращение к находящимся внутри русурсам должно осуществляться через него.

  > <code>src/editor/components/<b>editor</b>/component.jsx</code>:
  >
  > ```js
  > // ✅ так можно
  >
  > import Actions from "../actions";
  >
  > // ❌ так нельзя, прямое обращение ресурсам юнита
  >
  > import mediaIcon from "../actions/assets.media-icon.svg";
  > ```

### Дополнительно

- Для именования директорий и файлов рекомендуется использовать kebab-case.

## FAQ

**Можно ли использовать Sliced Design с TypeScript?**

Да!

**Можно ли использовать Sliced Design с CommonJS?**

Да! Только синтаксис модулей будет отличаться.

**Можно ли использовать Sliced Design с другими языками?**

Полагаю, что да! Только система модулей в вашем языке может значительно отличаться. Если у вас получится, пожалуйста, поделитесть опытом.

## Обратная связь

Буду рад любой обратной связи! Пожалуйста, отправляйте её в discussions или issues.

## Контрибьюторы

- [@austrokhart](https://github.com/austrokhart)
- [@daryabratova](https://github.com/daryabratova)
- [@a1ex-kaufmann](https://github.com/a1ex-kaufmann)
- [@kindaro](https://github.com/kindaro)

## Поддержать автора

[Boosty](https://boosty.to/austrokhart)
